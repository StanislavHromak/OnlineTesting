@model Discipline

@{
    ViewData["Title"] = "Видалення дисципліни";
}

<div class="container pt-4">
    <h2 class="text-danger mb-4">🗑️ Видалити дисципліну</h2>
    <p>Ви впевнені, що хочете видалити дисципліну <strong>@Model.Name</strong>?</p>

    <form asp-action="Delete" method="post" class="mt-4">
        <input type="hidden" asp-for="Id" />

        <div class="d-flex justify-content-between">
            <a asp-action="Index" class="btn btn-secondary">← Назад</a>
            <button type="submit" class="btn btn-danger">🗑️ Підтвердити видалення</button>
        </div>
    </form>
</div>
